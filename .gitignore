
.DS_Store
# Editor / OS cruft
*.key
.lattice/
lattice-simulation/
lattice-storage*/
lattice-storage/
# Node identity secrets — never commit private keys
relay.log
# Rust build artifacts
state.snapshot
*.swp
/target
*.wal
z4.log
